import { Styles } from "src/types/common";

const styles: Styles = {
  wrapperBody: {
    typography: "bodyLayout",
    overflow: "auto",
    top: "70px",
    bottom: "30px",
  },
};

export default styles;
